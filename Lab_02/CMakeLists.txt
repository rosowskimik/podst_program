file(GLOB 02_FILES CONFIGURE_DEPENDS *.cpp)
foreach(source ${02_FILES})
    cmake_path(GET source FILENAME fileName)
    cmake_path(GET fileName STEM fileStem)
    add_executable("Lab_02_${fileStem}" ${fileName})
endforeach()
